import 'package:flutter/material.dart';

class Educational extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Educational'),
      ),
      body: Center(
        child: Text('This is the educational material screen'),
      ),
    );
  }
}
